touch-deploy:
    stage: deploy
    script:
        - apk add curl
        - "curl -X POST -F token=$ALU_SOI_TRIGGER_TOKEN -F ref=master https://gitlab.raccoongang.com/api/v4/projects/206/trigger/pipeline"
    only:
        - master
