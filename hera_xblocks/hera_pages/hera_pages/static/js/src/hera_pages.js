/* Javascript for HeraPagesXBlock. */
function HeraPagesXBlock(runtime, element) {
    $('.single-item').slick({
        slidesToShow: 1,
        slidesToScroll: 1,
        arrows: true,
        fade: true,
        // asNavFor: '.slider-nav'
      });
}


