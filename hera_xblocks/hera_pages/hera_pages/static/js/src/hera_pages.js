/* Javascript for HeraPagesXBlock. */
function HeraPagesXBlock(runtime, element) {
    $('.slidebar-wrapper').slick({
        slidesToShow: 1,
        slidesToScroll: 1,
        arrows: true,
        fade: true,
        adaptiveHeight: true
        // asNavFor: '.slider-nav'
      });
}


